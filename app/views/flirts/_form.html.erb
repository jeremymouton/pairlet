<%= form_for(@flirt) do |f| %>
  <% if @flirt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flirt.errors.count, "error") %> prohibited this flirt from being saved:</h2>

      <ul>
      <% @flirt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :provider %><br />
    <%= f.select(:provider, options_for_select([['Select one', ''], ['Twitter', 'twitter'], ['Facebook', 'facebook']], 1)) %>
  </div>

  <div class="field">
    <%= f.label :handle %><br />
    <%= f.text_field :handle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
